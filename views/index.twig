{% extends "layout.twig" %}

{% block page %}
    <div class="well">
        <h1>{{"title_index"|trans}}</h1>
        <p class="lead">
            {{ "intro_index"|trans }}
        </p>
        <p><a href="{{ url('home') }}" class="btn btn-primary btn-large">{{"start"|trans}}</a></p>
    </div>
{% endblock %}
